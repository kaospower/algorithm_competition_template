**how to activate pytorch**

First, open the terminal.

Then, input conda activate torch-gpu.

After the interactive window appears, input jupyter notebook.

In the jupyter notebook, input: import torch and then begin to  call pytorch API.